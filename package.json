{
  "name": "park-conscious",
  "version": "1.0.0",
  "packageManager": "npm@10.2.4",
  "type": "module",
  "workspaces": [
    "apps/events",
    "apps/admin",
    "apps/api"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "mkdir -p public && cp web/*.html web/favicon.ico web/favicon.png web/sitemap.xml web/robots.txt public/ 2>/dev/null || true && cp -r web/assets web/scripts web/styles public/ 2>/dev/null || true && cp -r web/owner public/ 2>/dev/null || true && node -e \"const fs=require('fs'); const fn='public/scripts/config.js'; let c=fs.readFileSync(fn,'utf8'); c=c.replace('__MAPS_API_KEY__', process.env.GOOGLE_MAPS_API_KEY || '').replace('__ROUTING_API_URL__', process.env.ROUTING_API_URL || 'http://localhost:8000'); fs.writeFileSync(fn, c);\" && turbo build",
    "lint": "turbo lint",
    "dev:backend": "node --watch local-server.js",
    "test:events": "turbo test --filter=@parkconscious/events"
  },
  "devDependencies": {
    "babel-preset-react-app": "^10.0.1",
    "concurrently": "^9.2.1",
    "eslint-config-react-app": "^7.0.1",
    "turbo": "^2.9.12"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "bcryptjs": "^2.4.3",
    "busboy": "^1.6.0",
    "cloudinary": "^2.9.0",
    "cookie": "^1.1.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "ioredis": "^5.10.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "8.10.1",
    "nodemailer": "^8.0.7",
    "qrcode.react": "^4.2.0",
    "razorpay": "^2.9.6",
    "resend": "^6.10.0"
  }
}
